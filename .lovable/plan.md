## הסתרת כפתור "הנפק חשבונית מס קבלה"

הסתרה ויזואלית בלבד של הכפתור העצמאי ב-`src/components/orders/PaymentSection.tsx` (שורות 480-523).

**שינוי:** עטיפת הבלוק ב-`{false && (...)}` או הוספת `hidden` ל-className — כך הלוגיקה נשארת לשימוש עתידי אבל לא מוצגת.

**מה לא משתנה:**
- אין שינוי במסד הנתונים
- אין שינוי בלוגיקת ה-`CompleteOrderDialog` (זה שמייצר את החשבונית האוטומטית)
- הטוגל בתוך דיאלוג "רישום תשלום" נשאר כפי שהוא